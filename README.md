# Dog Facts API

A simple REST API built with Node.js and Express that returns dog facts.

# Installation

npm install

# Running the Server

node server.js

# Endpoints

 /facts  
Returns all dog facts.

 /facts?number=1  
Returns the specified number of dog facts.

# Error Handling

- Invalid number parameter
- Negative numbers
- Requesting more facts than available
